/**
 * @param matrix  2-dim jagged array
 * @returns {{path: *, letters: *}}
 */
const findPath = (matrix) => {

    if (!matrix || matrix.length === 0) return false;
    let letters, path;

    // FIND START

    // LOOK AROUND

    // is not valid start - exit

    // else:

    /// TAKE STEP

    /// is end - exit

    /// else:

    //// is intersection - keep direction -> back to TAKE STEP

    //// else :

    //// turn or keep direction -> back to TAKE STEP



    return {
        letters: letters,
        path: path
    };
};

module.exports = findPath;
